{% extends 'base.html.twig' %}

{% block title %}Kunde{% endblock %}

{% block body %}
    
    <h1 class="mb-4">Kunde {{ client.name }}</h1>

    <table class="table mb-4">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ client.name }}</td>
            </tr>
            <tr>
                <th>Domain</th>
                <td>{{ client.domain }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}">Zurück</a>
    <a href="{{ path('app_client_edit', {'id': client.id}) }}">Bearbeiten</a>

    {{ include('client/_delete_form.html.twig') }}

{% endblock %}
